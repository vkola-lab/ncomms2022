this folder contains the scripts for generating the model's performance tables (generate_metric_table.py)
and conducting the search of the optimal thresholds from a mesh grid (thresholding.py)

To use the thresholding.py, you simply need to run the "threshold_crossValid" function with the following
parameters being passed in:
    (1) valid_files: List[string]